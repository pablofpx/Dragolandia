package org.example;

import org.example.controller.CombateController;

public class Main {
    public static void main(String[] args) {
        CombateController controller = new CombateController();
        controller.iniciar();
    }
}